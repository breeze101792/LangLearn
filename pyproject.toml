[project]
name = "langlearn"
version = "0.1.0"
description = "Multi-language learning web app."
requires-python = ">=3.10"
dependencies = [
    "Flask>=3.0",
    "nltk>=3.8",
    "jsonschema>=4.21",
    "requests>=2.31",
]